@using Investor.Model
@model IEnumerable<BlogPreview>

@foreach (var blog in Model)
{
    <input hidden class="numberOfPosts" value="@Model.Count()"/>
    @Html.Partial("~/Views/Blog/_LatestBlogPreviewTemplate.cshtml", blog)
}