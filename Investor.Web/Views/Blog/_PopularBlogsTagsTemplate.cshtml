@model IEnumerable<Investor.Model.Tag>

<div class="wrapper-tag-news">
    @foreach (var tag in Model)
    {
        <a asp-action="SearchByTag" asp-controller="Search" asp-route-tag="@tag.Name" class="tag-item">@tag.Name</a>
    }
</div>